<h3>My Boards</h3>

<ul id="my-boards">
  <% boards.each(function (board) { %>
    <li>
      <a href="#/boards/<%= board.id %>">
        <%= board.escape('title') %>
      </a>
    </li>
  <% }); %>
</ul>
